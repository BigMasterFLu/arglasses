# Hand Tracking Virtual Keyboard - HIGH PERFORMANCE Requirements
# Optimized for 60 FPS performance on RTX 4050 with CUDA acceleration
# Updated to match current working environment

# CORE DEPENDENCIES (Essential packages)
opencv-python==4.10.0.84
opencv-contrib-python==4.11.0.86
mediapipe==0.10.9
numpy==1.24.3
PyAutoGUI==0.9.54

# TENSORFLOW & ML BACKEND (GPU acceleration support)
tensorflow==2.13.0
tensorflow-intel==2.13.0
keras==2.13.1

# MEDIAPIPE DEPENDENCIES (Required by MediaPipe)
absl-py==2.3.1
attrs==25.3.0
flatbuffers==25.2.10
protobuf==3.20.3
sounddevice==0.5.2

# PYAUTOGUI DEPENDENCIES (Required for virtual keyboard)
Pillow==11.3.0
PyGetWindow==0.0.9
PyMsgBox==2.0.1
PyRect==0.2.0
PyScreeze==1.0.1
pytweening==1.2.0
pyperclip==1.9.0
MouseInfo==0.1.3

# VISUALIZATION & PLOTTING (Required by MediaPipe)
matplotlib==3.10.6
contourpy==1.3.2
cycler==0.12.1
fonttools==4.59.2
kiwisolver==1.4.9
pyparsing==3.2.4
python-dateutil==2.9.0.post0
packaging==25.0
six==1.17.0

# NETWORKING & SECURITY (TensorFlow dependencies)
requests==2.32.5
urllib3==2.5.0
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10

# SYSTEM & UTILITIES
cffi==2.0.0
pycparser==2.23

# PERFORMANCE OPTIMIZATIONS APPLIED:
# - 640x480 camera resolution for faster processing
# - MediaPipe Lite model (model_complexity=0) for maximum speed
# - Frame skipping: MediaPipe processes every 2nd frame
# - Reduced drawing thickness and simplified FPS calculation
# - Lightweight keyboard overlay blending
# - CUDA environment variables for GPU optimization
# - Minimized cv2.waitKey() for reduced latency

# EXPECTED PERFORMANCE:
# - Target: 50-60 FPS (up from 25 FPS)
# - RTX 4050 GPU acceleration enabled
# - Optimized memory allocation and processing pipeline

# Installation instructions:
# 1. Create virtual environment: python -m venv venv
# 2. Activate environment: .\venv\Scripts\Activate.ps1 (Windows) or source venv/bin/activate (Linux/Mac)
# 3. Install dependencies: pip install -r requirements.txt
# 4. Run application: python main.py

# Note: JAX and JAXlib have been removed to fix NumPy compatibility issues