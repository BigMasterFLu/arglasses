# Hand Tracking Virtual Keyboard - Clean Requirements
# Conflict-free dependencies for the hand tracking application

# Core computer vision and hand tracking
opencv-python==4.10.0.84
mediapipe==0.10.9

# Numerical computing (compatible with MediaPipe's TensorFlow backend)
numpy==1.24.3

# GUI automation for virtual keyboard functionality
PyAutoGUI==0.9.54

# Additional dependencies will be automatically installed by MediaPipe:
# - absl-py (Google's Python common libraries)
# - attrs (classes without boilerplate)
# - flatbuffers (serialization library)
# - matplotlib (plotting library)
# - opencv-contrib-python (extended OpenCV functionality)
# - protobuf (protocol buffers)
# - sounddevice (audio I/O)
# - pillow (image processing)
# - and other supporting libraries

# Installation instructions:
# 1. Create virtual environment: python -m venv venv
# 2. Activate environment: .\venv\Scripts\Activate.ps1 (Windows) or source venv/bin/activate (Linux/Mac)
# 3. Install dependencies: pip install -r requirements.txt
# 4. Run application: python main.py